# 产品配置说明

仓库根目录的 `product.config.json` 是 APP、Admin 和 Backend 共享的非敏感产品配置。配置文件已经关联 JSON Schema，在 VS Code 中把鼠标放在字段上即可看到中文说明，填错格式时也会直接提示。

## 复制模板后的一键初始化

在仓库根目录执行：

```powershell
npm.cmd run product:init
```

命令会逐项询问产品名称、公司名称、Android 包名、线上 API 地址、内容类型、广告平台与广告位 ID，以及需要启用的业务模块。直接按 Enter 会保留当前值；确认后会完成以下操作：

1. 校验字段格式和模块依赖关系。
2. 将旧配置备份到已被 Git 忽略的 `.git-local-backups`。
3. 更新 `product.config.json`。
4. 自动同步 APP、Admin 和 Backend 的生成配置。

第一次使用建议先预览，不写入文件：

```powershell
npm.cmd run product:init -- --yes --dry-run --app-name="新剧场"
```

CI 或批处理可以使用非交互参数：

```powershell
npm.cmd run product:init -- --yes `
  --product-code=new-theater `
  --app-name="新剧场" `
  --admin-title="新剧场管理后台" `
  --company-name="示例科技有限公司" `
  --ad-app-name="广告平台注册应用名" `
  --application-id=com.example.theater `
  --admin-namespace=new-theater-admin `
  --api-origin=https://api.example.com `
  --module=invitations=false
```

查看所有参数：

```powershell
npm.cmd run product:init -- --help
```

初始化命令只处理非敏感配置，不会询问或写入数据库密码、AccessKey、支付宝私钥、签名密码等秘密信息。

## 部署前体检

本地开发阶段可以执行只读体检：

```powershell
npm.cmd run product:doctor
```

服务器上的 `deploy/.env` 和 `deploy/backend.env` 填写完成后，使用生产严格模式：

```bash
npm run product:doctor -- --production
```

需要同时验证线上 API 健康状态和 APP 下载页时执行：

```bash
npm run product:doctor -- --production --live
```

体检会检查三端生成配置、模块所需环境变量、接口文档与 CORS、MySQL 持久化配置、Android 签名、GroMore AAR、Git 敏感文件和大文件。它不会修改任何配置，也不会在输出中打印密码或私钥。CI 可以添加 `--json` 获得机器可读结果；有错误时命令退出码为 `1`，只有警告时退出码仍为 `0`。

修改后，在仓库根目录执行：

```powershell
node scripts/sync-product-config.mjs
```

也可以在 APP 目录执行：

```powershell
npm.cmd run sync:product
```

## 字段说明

| 字段 | 用途 | 修改注意事项 |
| --- | --- | --- |
| `schemaVersion` | 配置结构版本 | 固定为 `2`，不是 APP 版本号 |
| `productCode` | 产品内部英文标识 | 使用小写英文和连字符，发布后不建议修改 |
| `brand.appDisplayName` | APP 桌面和系统中显示的名称 | 修改后需要重新打包 APP |
| `brand.adminTitle` | 管理后台名称 | 重新构建 Admin 后生效 |
| `brand.adminDescription` | 管理后台页面描述 | 重新构建 Admin 后生效 |
| `brand.companyName` | 运营公司名称 | 用于协议、关于等展示位置 |
| `android.applicationId` | Android 应用唯一包 ID | 正式发布后修改会被识别成另一款 APP |
| `admin.namespace` | Admin 本地缓存命名空间 | 复制新项目时应修改，避免多个后台缓存冲突 |
| `content.type` | 内容类型 | 可选 `none`、`shortDrama`、`quiz`、`novel`、`music` |
| `content.providers.shortDrama.sdkSettingId` | 短剧内容 SDK Setting ID | 仅启用 `shortDrama` 时必填，与广告 Provider 独立 |
| `modules.advertising` | 广告能力 | 控制广告能力声明；关闭时 `advertising.provider` 必须为 `none` |
| `modules.rewards` | 金币、签到和广告收益 | 关闭前需要让福利、收益页面接管开关 |
| `modules.invitations` | 邀请码、分享和邀请任务 | 关闭前需要同步调整注册邀请码规则 |
| `modules.smsRegistration` | 阿里云短信验证码注册 | 没有其他注册验证方式时不要关闭 |
| `modules.withdrawals` | 用户提现及后台审核 | 关闭前需要隐藏 APP 和后台提现入口 |
| `modules.alipayPayout` | 支付宝自动打款和查单 | 开启时 `withdrawals` 必须同时开启 |
| `domains.productionApiOrigin` | 正式 API 根地址 | 只写 `https://域名`，不要带路径和结尾 `/` |
| `advertising.provider` | 当前广告平台 | 可选 `none`、`gromore`、`taku` |
| `advertising.providers.<provider>.registeredAppName` | 广告平台注册应用名 | 必须与当前平台登记信息一致，可与 APP 名称不同 |
| `advertising.providers.<provider>.appId` | 广告平台应用 ID | 当前选中的平台必须填写数字字符串 |
| `splashPlacementId` | 开屏广告位 ID | 必须对应当前广告平台后台广告位 |
| `feedPlacementId` | 信息流广告位 ID | 必须对应当前广告平台后台广告位 |
| `fullScreenPlacementId` | 全屏/插屏广告位 ID | 必须对应当前广告平台后台广告位 |
| `rewardPlacementId` | 激励视频广告位 ID | 当前任务激励和内容解锁共用此广告位 |

## 哪些修改需要重新打包

- APP 名称、Android 包名、SDK 应用名、广告应用 ID 或广告位 ID：需要重新打包 APP。
- Admin 名称、描述、缓存命名空间、API 地址：需要重新构建并上传 Admin。
- 公司名称：同步后，根据实际使用端重新构建相应项目。

`MobileReactNative/product.generated.json` 是脚本生成结果，业务代码和 Gradle 从中读取配置，请勿手动修改。

模块开关现已接管 APP 导航、Admin 菜单和 Backend 接口；Backend 也会通过 `/api/v1/operations/bootstrap` 下发实际能力。依赖关系如下：

- `content.type` 决定内容插件；未选择短剧时不得出现短剧菜单、页面和接口。
- `advertising.provider` 决定 APP 使用的广告适配器，业务页面不得直接依赖厂商 SDK。

- `invitations` 依赖 `rewards`。
- `withdrawals` 依赖 `rewards`。
- `alipayPayout` 依赖 `withdrawals`。
- `smsRegistration` 关闭后只保留已有账号登录；没有替代注册方式时，新用户将无法注册。

只校验某种模块组合、不改写任何生成文件时，可以执行：

```powershell
node scripts/sync-product-config.mjs --check --module=invitations=false
```

## 禁止写入的敏感配置

以下内容必须继续使用服务器环境变量、服务器密钥或本地未提交文件：

- 数据库密码、JWT 密钥
- 阿里云 AccessKey ID 和 AccessKey Secret
- 支付宝应用私钥
- GroMore 服务端回调安全密钥
- Android 签名库密码和密钥密码

修改 Android `applicationId` 不要求同步修改当前 Kotlin `namespace`。如果确实需要迁移原生代码包名，应作为单独任务处理。
