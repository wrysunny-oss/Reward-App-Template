# 奖励应用模板生产部署操作指南

本文档说明奖励应用模板项目从代码到生产服务器的完整部署流程，适用于 Alibaba Cloud Linux 3、宝塔 Nginx、Docker Compose、MySQL 8.4。

## 1. 部署架构

```text
用户浏览器 ──HTTPS──> Nginx ──> Admin 静态文件
APP/管理后台 ──HTTPS──> Nginx ──> Backend 容器 :3000 ──> MySQL 容器
                                           ├──> 支付宝
                                           ├──> 阿里云短信
                                           └──> 穿山甲回调
```

- Backend 与 MySQL 使用 Docker Compose 运行。
- Admin 构建为静态文件，由宝塔 Nginx 提供。
- APP 构建为 APK 或 AAB 后安装、测试或发布。
- Nginx 是唯一对公网提供 Web 服务的入口。

## 2. 部署前准备

服务器需要准备：

- Docker 与 Docker Compose
- 宝塔面板或 Nginx
- `api.example.com` 指向服务器公网 IP
- `admin.example.com` 指向服务器公网 IP
- 两个域名的 HTTPS 证书
- 支付宝证书、应用私钥和上线应用配置
- 阿里云短信 RAM AccessKey、签名和验证码模板
- 穿山甲回调及验签配置

安全组只需要向公网开放：

- TCP 80
- TCP 443
- SSH 管理端口，建议只允许可信 IP

不要向公网开放：

- 3000：Backend 端口
- 3306：MySQL 端口

## 3. 服务器目录规划

```text
/opt/hanle/app            项目源码
/opt/hanle/certs          支付宝证书
/data/hanle/mysql         MySQL 持久化数据
/data/hanle/uploads       用户上传文件
/data/hanle/backups       数据库和发布备份
```

创建目录：

```bash
mkdir -p /opt/hanle/app
mkdir -p /opt/hanle/certs
mkdir -p /data/hanle/mysql
mkdir -p /data/hanle/uploads
mkdir -p /data/hanle/backups
```

其中 `/opt/hanle/app` 可以随版本更新，`/data/hanle` 保存不能随容器删除的生产数据。

## 4. 获取项目代码

首次部署：

```bash
cd /opt/hanle
git clone https://github.com/wrysunny-oss/Theater_APP.git app
cd /opt/hanle/app
```

如果仓库是私有仓库，需要使用 GitHub Deploy Key 或 Personal Access Token。

以后更新代码：

```bash
cd /opt/hanle/app
git status
git pull --ff-only origin main
```

服务器上的项目源码原则上不要直接修改。`--ff-only` 可以避免服务器自动产生合并提交。

## 5. 配置生产环境变量

进入部署目录并从模板创建正式配置：

```bash
cd /opt/hanle/app/deploy
cp .env.example .env
cp backend.env.example backend.env
chmod 600 .env backend.env
```

### 5.1 `deploy/.env`

该文件供 Docker Compose 使用，主要配置数据库和持久化目录：

```env
MYSQL_DATABASE=reward_app_template
MYSQL_USER=hanle_app
MYSQL_PASSWORD=替换为数据库普通用户随机密码
MYSQL_ROOT_PASSWORD=替换为数据库root随机密码
DATA_ROOT=/data/hanle
CERT_ROOT=/opt/hanle/certs
```

可以使用以下命令生成随机密码：

```bash
openssl rand -hex 24
```

### 5.2 `deploy/backend.env`

该文件供 Backend 使用，主要包含：

- JWT Access Token 和 Refresh Token 密钥
- 提现数据加密密钥
- 穿山甲回调、验签和广告配置
- 支付宝 App ID、私钥、证书及转账场景
- 阿里云短信 AccessKey、签名和模板
- CORS 和品牌配置

生产环境至少确认：

```env
NODE_ENV=production
API_DOCS_ENABLED=false
PORT=3000
APP_BRAND_NAME=奖励应用模板
CORS_ORIGINS=https://admin.example.com
ADMIN_ACCESS_TOKEN_EXPIRES_IN=24h
REFRESH_TOKEN_EXPIRES_IN=30d
```

管理后台访问令牌默认保留 24 小时，刷新令牌用于静默续签。修改令牌时长或密钥后需要重新创建 Backend 容器；已经签发的令牌不会被自动延长。

不能保留模板中的以下占位内容：

```text
replace_with_...
SMS_XXXXXXXXX
```

支付宝私钥需要写成单行，换行使用字面量 `\n`：

```env
ALIPAY_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\n私钥正文\n-----END PRIVATE KEY-----
```

真实的 `.env`、`backend.env`、私钥和正式签名文件不能提交到 GitHub。

## 6. 上传支付宝证书

将三个证书上传到：

```text
/opt/hanle/certs/appCertPublicKey.crt
/opt/hanle/certs/alipayCertPublicKey_RSA2.crt
/opt/hanle/certs/alipayRootCert.crt
```

对应环境变量：

```env
ALIPAY_APP_CERT_PATH=certs/appCertPublicKey.crt
ALIPAY_PUBLIC_CERT_PATH=certs/alipayCertPublicKey_RSA2.crt
ALIPAY_ROOT_CERT_PATH=certs/alipayRootCert.crt
```

Docker 会把服务器的 `/opt/hanle/certs` 只读映射到容器内的 `/app/certs`。

## 7. 首次启动 MySQL

```bash
cd /opt/hanle/app/deploy

docker compose \
  --env-file .env \
  -f docker-compose.production.yml \
  up -d mysql
```

查看状态：

```bash
docker compose \
  --env-file .env \
  -f docker-compose.production.yml \
  ps
```

MySQL 最终应显示为 `healthy`。

查看 MySQL 日志：

```bash
docker compose \
  --env-file .env \
  -f docker-compose.production.yml \
  logs --tail=200 mysql
```

MySQL 数据实际保存在 `/data/hanle/mysql`。重新创建容器不会自动清空数据，但数据持久化不等于备份。

## 8. 执行数据库迁移

```bash
docker compose \
  --env-file .env \
  -f docker-compose.production.yml \
  --profile tools \
  run --rm migrate
```

该命令会临时启动迁移容器，等待 MySQL 健康，执行 `prisma migrate deploy`，然后删除临时容器。

看到以下提示表示迁移成功：

```text
All migrations have been successfully applied.
```

生产环境不要运行 `prisma migrate dev`。`migrate dev` 用于开发环境生成迁移，生产环境只使用 `migrate deploy`。

## 9. 构建并启动 Backend

```bash
docker compose \
  --env-file .env \
  -f docker-compose.production.yml \
  up -d --build backend
```

Backend 镜像构建过程包括：

1. 安装 Node.js 依赖。
2. 生成 Prisma Client。
3. 编译 TypeScript。
4. 创建只包含生产依赖的运行镜像。
5. 以非 root 用户启动 Node.js 服务。

Backend 只绑定到本机：

```text
127.0.0.1:3000
```

验证本机服务：

```bash
curl http://127.0.0.1:3000/ready
```

正常结果类似：

```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "status": "ready",
    "database": "up"
  }
}
```

查看后端日志：

```bash
docker compose \
  --env-file .env \
  -f docker-compose.production.yml \
  logs -f --tail=200 backend
```

按 `Ctrl+C` 只会退出实时日志，不会停止容器。

## 10. 配置 API 域名

在宝塔中将 `api.example.com` 反向代理到本机 Backend：

```nginx
client_max_body_size 6m;

location / {
    proxy_pass http://127.0.0.1:3000;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_connect_timeout 10s;
    proxy_read_timeout 60s;
    proxy_send_timeout 60s;
}
```

保存前检查 Nginx 配置：

```bash
nginx -t
```

重载 Nginx 后验证外网地址：

```bash
curl https://api.example.com/ready
```

如果本机地址能访问而 HTTPS 地址不能访问，应依次检查：

1. Nginx 反向代理配置。
2. 域名 DNS 解析。
3. HTTPS 证书。
4. 阿里云安全组。
5. 宝塔防火墙。

## 11. 构建并发布 Admin

建议在开发电脑构建 Admin，服务器只提供静态文件。

Admin 当前要求：

- Node.js 22.18 或更高版本
- pnpm 11

安装依赖并构建：

```powershell
cd D:\Project\Reward-App-Template\Admin
corepack enable
pnpm install --frozen-lockfile
pnpm --filter @vben/web-naive run build
```

生产 API 地址位于：

```text
Admin/apps/web-naive/.env.production
```

当前应配置为：

```env
VITE_GLOB_API_URL=https://api.example.com/api/v1
```

构建结果：

```text
Admin/apps/web-naive/dist/
```

把 `dist` 里面的文件上传到：

```text
/www/wwwroot/admin.example.com
```

正确结构：

```text
/www/wwwroot/admin.example.com/index.html
/www/wwwroot/admin.example.com/assets/...
```

不要形成多余的 `dist` 层级：

```text
/www/wwwroot/admin.example.com/dist/index.html
```

Admin Nginx 配置：

```nginx
location / {
    try_files $uri $uri/ /index.html;
}

location ~* \.(?:css|js|mjs|png|jpg|jpeg|gif|webp|svg|ico|woff2?)$ {
    expires 7d;
    add_header Cache-Control "public, max-age=604800, immutable";
    try_files $uri =404;
}
```

`try_files ... /index.html` 用于避免刷新前端路由时出现 404。

## 12. 配置穿山甲回调

穿山甲后台的激励视频服务端回调地址：

```text
https://api.example.com/api/v1/webhooks/pangle/reward
```

回调链路：

```text
用户看完广告
   ↓
穿山甲服务器请求 API 域名
   ↓
Nginx 转发给 Backend
   ↓
Backend 验签和防重复
   ↓
写入广告记录及金币流水
   ↓
APP 查询奖励结果并刷新余额
```

生产环境不要继续使用临时的 `trycloudflare.com` 回调地址。

## 13. 构建 APP 正式包

版本号修改、APK/AAB 区别、后台升级策略和回滚流程统一参见 [APP 版本发布指南](docs/APP版本发布指南.md)。

在开发电脑执行：

```powershell
cd D:\Project\Reward-App-Template\MobileReactNative\android

.\gradlew.bat bundleRelease `
  -PAPP_ENV=production `
  -PAPP_API_ORIGIN=https://api.example.com
```

生成文件：

```text
MobileReactNative/android/app/build/outputs/bundle/release/app-release.aab
```

如果通过官网提供直接下载安装，构建 APK：

```powershell
.\gradlew.bat assembleRelease `
  -PAPP_ENV=production `
  -PAPP_API_ORIGIN=https://api.example.com
```

APK 输出位置：

```text
MobileReactNative/android/app/build/outputs/apk/release/app-release.apk
```

正式打包前检查：

- `app.json.displayName` 是用户看到的 APP 名称。
- `ad-config.ts.sdkAppName` 是穿山甲 SDK 应用名称。
- API 地址必须是正式 HTTPS 地址。
- 使用正式发布签名。
- 每次发布的 `versionCode` 必须递增。

APP 不会因为服务器执行 `git pull` 自动更新，必须重新构建并发布安装包。

## 14. 后续版本迭代

### 14.1 更新 Backend

先备份数据库，再拉取代码：

```bash
cd /opt/hanle/app
git pull --ff-only origin main

cd deploy

docker compose \
  --env-file .env \
  -f docker-compose.production.yml \
  build backend migrate

docker compose \
  --env-file .env \
  -f docker-compose.production.yml \
  --profile tools \
  run --rm migrate

docker compose \
  --env-file .env \
  -f docker-compose.production.yml \
  up -d backend
```

验证：

```bash
docker compose --env-file .env -f docker-compose.production.yml ps
curl http://127.0.0.1:3000/ready
curl https://api.example.com/ready
```

### 14.2 更新 Admin

开发电脑重新构建：

```powershell
cd Admin
pnpm --filter @vben/web-naive run build
```

上传前备份服务器旧版：

```bash
cp -a \
  /www/wwwroot/admin.example.com \
  /data/hanle/backups/admin-$(date +%Y%m%d-%H%M%S)
```

然后使用新的 `dist` 内容替换站点文件。

### 14.3 更新 APP

按照 [APP 版本发布指南](docs/APP版本发布指南.md) 递增 `versionCode`，重新构建 APK 或 AAB，在真机完成回归测试后发布新版本，并在管理后台新增对应版本策略。

## 15. 数据库备份

备份命令：

```bash
cd /opt/hanle/app/deploy

docker compose \
  --env-file .env \
  -f docker-compose.production.yml \
  exec -T mysql \
  sh -c 'exec mysqldump -uroot -p"$MYSQL_ROOT_PASSWORD" "$MYSQL_DATABASE"' \
  > /data/hanle/backups/hanle-$(date +%Y%m%d-%H%M%S).sql
```

检查备份文件：

```bash
ls -lh /data/hanle/backups
```

生产数据库应配置每日逻辑备份，并把备份同步到另一台服务器或对象存储。同一块服务器硬盘上的备份不能抵御硬盘损坏。

## 16. 常用维护命令

查看服务：

```bash
docker compose --env-file .env -f docker-compose.production.yml ps
```

查看 Backend 日志：

```bash
docker compose --env-file .env -f docker-compose.production.yml logs --tail=200 backend
```

查看 MySQL 日志：

```bash
docker compose --env-file .env -f docker-compose.production.yml logs --tail=200 mysql
```

实时查看 Backend 日志：

```bash
docker compose --env-file .env -f docker-compose.production.yml logs -f --tail=200 backend
```

Nginx API 错误日志：

```bash
tail -n 200 -f /www/wwwlogs/api.example.com.error.log
```

更完整的定位顺序参见 [线上故障排查手册](docs/线上故障排查手册.md)。

重启 Backend：

```bash
docker compose --env-file .env -f docker-compose.production.yml restart backend
```

重新构建 Backend：

```bash
docker compose --env-file .env -f docker-compose.production.yml up -d --build backend
```

查看服务器资源：

```bash
docker stats
df -h
free -h
```

## 17. 上线后核验清单

- [ ] `docker compose ps` 中 MySQL 和 Backend 均为 healthy。
- [ ] `https://api.example.com/ready` 返回正常。
- [ ] `https://admin.example.com` 能正常登录。
- [ ] 管理后台数据看板和配置页面正常。
- [ ] 新账号能收到阿里云注册验证码并完成注册。
- [ ] 福利中心激励广告可以正常获得奖励。
- [ ] 短剧广告完整观看后可以解锁剧集。
- [ ] 福利中心和短剧解锁广告共用每日收益次数限制。
- [ ] 达到每日上限后不再发金币，但短剧广告仍可解锁剧集。
- [ ] 穿山甲服务端回调能够进入 Backend。
- [ ] 广告收益、金币流水和用户余额保持一致。
- [ ] 支付宝最小提现档位可以打款并查询结果。
- [ ] 上传图片和静态资源访问正常。
- [ ] APP 使用正式 HTTPS API，而不是本地或内网穿透地址。

## 18. 部署原则

1. 代码可以重新拉取，数据和密钥不能丢失。
2. `.env`、私钥和正式签名文件不能进入 Git。
3. MySQL 3306 和 Backend 3000 不向公网开放。
4. 数据库结构修改必须通过 Prisma migration。
5. 发布 Backend 前先备份，再迁移，再启动。
6. 每次发布后检查 `/ready`、容器状态和日志。
7. 支付宝先做最小金额单笔测试，不要直接批量处理全部提现。
8. Docker 数据持久化不等于数据库备份。
