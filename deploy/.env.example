# 复制为 deploy/.env；只使用字母和数字可避免 DATABASE_URL 转义问题。
MYSQL_DATABASE=hanle_theater
MYSQL_USER=hanle_app
MYSQL_PASSWORD=replace_with_a_long_random_password
MYSQL_ROOT_PASSWORD=replace_with_another_long_random_password

# 生产数据统一落到独立持久化目录。
DATA_ROOT=/data/hanle
CERT_ROOT=/opt/hanle/certs
